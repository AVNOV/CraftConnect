import ProfileButton from "./ProfileButton";

export default function Header() {
  return (
    <header>
      <nav>
        <span>CraftConnect</span>
        <ProfileButton />
      </nav>
    </header>
  );
}
